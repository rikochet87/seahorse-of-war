{
"scripts": {
  "dev": "parcel serve ./src/index.html",
  "build": "parcel build ./src/index.html --dist-dir build --public-url /seahorse-of-war",
  "deploy": "gh-pages -d build",
  "build-and-deploy": "npm run build && npm run deploy"
},

  "dependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  }
}